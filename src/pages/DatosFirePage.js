import { Container } from "react-bootstrap";

const DatosFirePage = () =>  {
    return <h1>
        <Container>
            DatosFirePage
        </Container>
        </h1>;
};

export default DatosFirePage;